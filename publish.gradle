apply plugin: 'org.ajoberstar.github-pages'

githubPages {
  repoUri = 'git@github.com:saladinkzn/saladinkzn.github.io.git'
  targetBranch = 'master'
  pages {
    from(file('build/jbake')) {
      into '.'
    }
  }
}

project.tasks.publishGhPages.dependsOn project.tasks.jbake